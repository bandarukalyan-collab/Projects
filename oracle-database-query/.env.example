# Oracle Database Configuration
# This tool uses OS Authentication (SSO), so username/password are not required
# Multi-database configurations are managed in config.py

# To add a new database:
# 1. Add entry to DATABASES dictionary in config.py
# 2. Include tns_alias and tns_string for the database
# 3. Ensure TNS alias is configured in your tnsnames.ora
