# Project Constitution

> **Purpose:** This document defines the non-negotiable engineering principles, quality gates, conventions, and governance rules for this workspace. All AI agents and human developers must comply with these rules when generating specs, plans, or code.
>
> **Scope:** Applies to every feature delivered in this workspace and all projects cloned under it.
>
> **Precedence:** If anything in a feature spec, implementation plan, or code conflicts with this document, this document wins. Violations block `/create-plan` generation.
>
> **Technology stack** is not governed here — it is captured in `AGENTS.md` (auto-generated by `/create-app-knowledge` from the actual codebase).

---

## 1. Core Principles

Numbered, non-negotiable rules. Each principle must be testable and enforceable.

1. **Spec-first** — no production code is written without an approved feature spec (`specs.md`) and implementation plan (`plan.md`). Exploratory spikes are fine, but must be discarded or re-implemented under a spec before merging.
2. **TDD (Test-Driven Development)** — tests are written before the implementation they verify. Every functional requirement (FR) and acceptance criterion (AC) must map to at least one automated test.
3. **End-to-end traceability** — every FR maps to a concrete file change in the plan; every AC maps to a test case. The full chain is: JIRA → Feature Spec → Implementation Plan → Commits → Tests → Change Summary → JIRA Comment → Change Log → Confluence. Gaps at any link are blockers, not warnings.
4. **Backward compatibility** — public APIs, DTOs, and persisted data schemas evolve additively. Breaking changes require an explicit, documented migration plan and stakeholder sign-off.
5. **Single source of truth** — shared knowledge lives in `ai/knowledge/` and is updated by the `/update-knowledge` skill. Do not duplicate domain facts across feature specs.
6. **No secrets in code** — credentials, tokens, and PII never enter Git. Use `local.config` (gitignored) or the approved secrets manager.
7. **Small, reviewable changes** — a Merge Request addresses one JIRA issue. Prefer multiple focused MRs over one large MR.
8. **Acceptance criteria are immutable** — ACs in feature specs must be preserved verbatim from JIRA. Never rephrased, reordered, or summarised.
9. **Concrete references only** — implementation plans must contain actual file paths, exact method names, and approximate line numbers. No placeholders (`TBD`, `{path}`, etc.).
10. **Permanent retention** — all feature documents (`specs.md`, `plan.md`, `change-summary.md`) and change-log entries are retained permanently in the `ai/` repository.
11. **No invented knowledge** — an agent shall not invent or assume any knowledge. If gaps, ambiguities, or contradictions are identified, the agent must ask a clarifying question and never assume or invent the answer. All facts must be traced to and backed by objective evidence.

---

## 2. Quality Gates

Hard gates. CI and/or the `/execute` skill must verify these before an MR is eligible for merge.

| Gate | Check | Blocking? |
|------|-------|-----------|
| Unit tests | All unit tests pass locally and in CI | YES |
| E2E / integration tests | All E2E tests pass where applicable | YES |
| Cross-project verification | Full test suite across all impacted projects passes | YES |
| Lint / format | Project linter and formatter pass with zero errors | YES |
| Type check | Static type check passes (where the language supports it) | YES |
| Build | Project builds cleanly with no new warnings introduced by the change | YES |
| Coverage | New/changed code meets the project's coverage threshold | YES |
| Traceability | Every FR → file change, every AC → test case | YES |
| Open questions | No unresolved open questions, unverified assumptions, or invented facts in spec/plan — every claim traces to objective evidence | YES |
| Secrets scan | No secrets, tokens, or credentials committed | YES |
| Post-implementation validation | Every FR is implemented, every AC is met with traceable test evidence, scope boundaries respected, NFRs satisfied, Definition of Done checklist passes | YES |
| Automated self-review | Git diff reviewed for unintended changes, secrets, and debug code | YES |
| Constitution compliance | Plan and code comply with every principle above | YES |

---

## 3. Workspace Standards

Every workspace must contain the following:

| Requirement | Path | Purpose |
|-------------|------|---------|
| AI knowledge repository | `ai/` (separate Git repo) | Structured knowledge base: domain docs, AI-generated specs, feature artefacts |
| Workspace context file | `AGENTS.md` (workspace root) | Auto-generated full project context for AI agents (via `/create-app-knowledge`) |
| Secondary memory | `LEARNINGS.md` (workspace root) | Persistent cross-session AI memory — gotchas, recipes, learnings |
| Personal access tokens | `local.config` (workspace root, gitignored) | `JIRA_PAT`, `GIT_PAT`, optional `CONFLUENCE_PAT` and `CONFLUENCE_PARENT` |
| Human domain docs | `ai/raw/` | Human-authored domain documentation (input to `/create-app-knowledge`) |
| AI-generated specs | `ai/knowledge/` | 8 application spec documents — regenerated quarterly or after major changes |

After every feature merge, `AGENTS.md`, `ai/knowledge/`, and `LEARNINGS.md` must be updated (via `/update-knowledge`).

---

## 4. Feature Development Standards

### 4.1 Feature Artefacts

Every feature must produce exactly 3 documents in `ai/specs/{ISSUE_ID}/`:

| Document | Contents |
|----------|----------|
| `specs.md` | 10 mandatory sections: Header, Overview, Background, Scope, Functional Requirements, Acceptance Criteria, NFRs, Impacted Areas, Open Questions, Definition of Done |
| `plan.md` | Actual file paths, exact method names, approximate line numbers, TDD implementation order, "Files NOT to Modify" section |
| `change-summary.md` | Per-project impact tables, test results, commit hashes, verification checklists, and MR links (project + URL + status) |

Both `specs.md` and `plan.md` must be attached to the JIRA issue before coding begins.

### 4.2 JIRA Integration

- Issues follow a 5-state workflow: **Proposed → Defining Details → Waiting for Dev → In Development → Complete**.
- Structured summary comments must be posted at pre-implementation and post-implementation milestones.
- The `AI-Delivered` label must be applied to every issue completed through the SDLC harness.

### 4.3 Human Review Checkpoints

The SDLC must pause at exactly **3 checkpoints**:

| # | After | Reviewer | Purpose |
|---|-------|----------|---------|
| 1 | Feature spec generation | POD Lead | Reviews and approves `specs.md` — validates scope aligns with Feature intent and Spec is ready for development |
| 2 | Implementation plan generation | Developer (author) | Reviews and approves `plan.md` — confirms file changes, TDD order, and traceability |
| 3 | MR creation | At least one team member (not the author) | Reviews code diffs in GitLab |

Before Checkpoint 1, the AI agent must attempt up to 3 iterations of autonomous clarification to self-resolve ambiguities.

### 4.4 Spec Requirements

#### 4.4.1 Quality Rules
- **Functional Requirements**: atomic (one testable behavior per row), testable (clear pass/fail outcome), traceable (source column references JIRA or app-spec), implementation-free (no code-level constructs)
- **Acceptance Criteria**: preserved verbatim from JIRA, never rephrased, reordered, or summarized
- **Success Criteria**: user-focused, technology-agnostic (no database/framework names), measurable and verifiable, business-oriented
- **Scope**: both "In Scope" and "Out of Scope" lists must be non-empty

#### 4.4.2 Quality Validation
Before proceeding to implementation, all 8 checks must pass:
| # | Check | PASS Criteria |
|---|-------|---------------|
| 1 | No implementation details leaked | No code-level constructs in requirement statements; code references only in Impacted Areas |
| 2 | All FRs are testable | Every FR has a clear pass/fail outcome — no vague language |
| 3 | Success criteria are technology-agnostic | No database, framework, or infrastructure names in NFRs |
| 4 | Scope is clearly bounded | Both In Scope and Out of Scope lists are non-empty |
| 5 | All FRs have sources | Every FR row has a traceable Source column |
| 6 | Acceptance criteria preserved | JIRA acceptance criteria appear verbatim (not paraphrased) |
| 7 | No orphan requirements | Every FR maps to at least one entry in the Impacted Areas table |
| 8 | Open questions are genuinely open | No question can be answered from existing JIRA/codebase/spec evidence; no facts are invented or assumed — agent must surface gaps as clarifying questions |

---

## 5. Version Control Conventions

### 5.1 Branching
- Default branch: `develop`. Release/protected branch: `main`.
- Feature branches: `develop-{ISSUE_ID}`.
- Never force-push to `develop` or `main`. Never commit directly to `main`.
- Every code change merges to `develop` via a Merge Request targeting `develop`.

### 5.2 Commit Messages
- Format: `JIRA#{ISSUE_ID}; {short description}` — no AI attribution.
- Operational commits outside a feature workflow use the reserved ID `JIRA-0000`.

### 5.3 Merge Requests
- Title format: `JIRA#{ISSUE_ID}; {JIRA Summary}`.
- Must always target `develop` — never `main` or `master`.
- Wrap-up must verify every MR is merged before deleting branches — open MRs halt the workflow.

### 5.4 Code & Design
- Follow existing patterns in the target project. Prefer extending over rewriting.
- Public APIs (REST, gRPC, DTOs) are documented in `ai/knowledge/api-spec.md`.
- Data model changes are reflected in `ai/knowledge/data-model-spec.md` before implementation.

### 5.5 Documentation Paths
- Feature specs: `ai/specs/{ISSUE_ID}/specs.md`
- Implementation plans: `ai/specs/{ISSUE_ID}/plan.md`
- Change summaries: `ai/specs/{ISSUE_ID}/change-summary.md`

---

## 6. Documentation & Publishing Standards

- A Confluence page (13 sections, standardised structure) must be published for every completed feature (when Confluence is configured via `local.config`).
- Every Confluence page must include a PlantUML sequence diagram showing the primary happy-path flow with real class names.

---

## 7. Error Handling Standards

| Category | Behaviour |
|----------|-----------|
| Build failures, test failures (after 3 retries), open MRs during wrap-up, missing specs | **Blocking** — halts the workflow |
| JIRA API failures, Confluence API failures, GitLab MR creation failures, knowledge update failures | **Non-blocking** — logs a warning and continues |

---

## 9. 12-Factor Principles

All applications governed by this constitution SHOULD adhere to the [12-Factor App] methodology for portability, resilience, and cloud-native deployment.

| # | Factor | Principle | Guidance |
|---|--------|-----------|----------|
| I | **Codebase** | One codebase tracked in revision control, many deploys | Single repository per application. Environment-specific differences handled through configuration, not code branches. |
| II | **Dependencies** | Explicitly declare and isolate dependencies | All dependencies MUST be declared in a manifest (e.g., `package.json`, `requirements.txt`). Never rely on implicit system-level packages. |
| III | **Config** | Store config in the environment | Configuration that varies between deploys (credentials, endpoints, feature flags) MUST be injected via environment variables or secure config service — never hard-coded. |
| IV | **Backing Services** | Treat backing services as attached resources | Databases, message queues, caches, and external APIs MUST be referenced by configuration and swappable without code changes. |
| V | **Build, Release, Run** | Strictly separate build and run stages | Build stage produces an artifact, release stage combines it with config, run stage executes it. These stages MUST NOT be mixed. |
| VI | **Processes** | Execute the app as one or more stateless processes | Application processes MUST be stateless and share-nothing. Persistent state MUST be stored in a backing service (database, cache, object store). |
| VII | **Port Binding** | Export services via port binding | Application MUST be self-contained and expose its service by binding to a port. MUST NOT depend on runtime injection of a web server. |
| VIII | **Concurrency** | Scale out via the process model | Applications MUST be designed to scale horizontally by running multiple stateless processes, not by increasing resources of a single process. |
| IX | **Disposability** | Maximize robustness with fast startup and graceful shutdown | Processes MUST start quickly and shut down gracefully on SIGTERM. Design for crash resilience — work should be idempotent or transactional. |
| X | **Dev/Prod Parity** | Keep development, staging, and production as similar as possible | Minimize gaps in tooling, backing services, and configuration between environments. Use the same database engine, message broker, etc. across all stages. |
| XI | **Logs** | Treat logs as event streams | Applications MUST NOT manage log files. Logs MUST be written to `stdout`/`stderr` and captured by the execution environment for aggregation and analysis. |
| XII | **Admin Processes** | Run admin/management tasks as one-off processes | Database migrations, console sessions, and maintenance scripts MUST run in an identical environment using the same codebase and config as the application. |

---

## 8. Amendment Process

This constitution changes only via an explicit, reviewable process.

1. Open a JIRA issue proposing the amendment (rationale, impact, migration).
2. Update `ai/CONSTITUTION.md` on a feature branch `develop-{ISSUE_ID}`.
3. Create an MR targeting `develop`. Reviewers: POD lead + at least one senior engineer.
4. On merge, run `/update-knowledge` so downstream skills pick up the change.

---

_Last updated: {YYYY-MM-DD} — {author}_
