# Integration Specification — Greeting Service

> Auto-generated by `/create-pod-knowledge`. Updated by `/update-knowledge`.

## 1. Overview

The Greeting Service has a simple integration model: the Node.js frontend and C# CLI both call the Java REST API over HTTP.

## 2. Integration Points

| Consumer | Provider | Protocol | Endpoint | Purpose |
|----------|----------|----------|----------|---------|
| hello-world-nodejs | hello-world-java | HTTP/REST | `GET /api/greeting` | Fetch greeting for display |
| hello-world-csharp | hello-world-java | HTTP/REST | `GET /api/greeting` | Fetch greeting for CLI output |

## 3. External Dependencies

None currently. All services are self-contained.

## 4. Authentication

No authentication is required between services in the current implementation. *(Planned for future releases.)*
