# Technical Specification — Greeting Service

> Auto-generated by `/create-pod-knowledge`. Updated by `/update-knowledge`.

## 1. Architecture

Three independent services, each in its own Git repository:

```
hello-world-nodejs (web frontend)
        |
        v HTTP
hello-world-java (REST API)  <-- hello-world-csharp (CLI tool)
```

## 2. Service Details

### hello-world-java (Greeting API)
- **Language**: Java 17
- **Build**: Maven (`mvn clean package`)
- **Entry point**: `com.example.App.main()`
- **Output**: Executable JAR
- **Port**: 8080 (default)

### hello-world-nodejs (Web Frontend)
- **Language**: JavaScript (ES2022)
- **Build**: `npm install`
- **Entry point**: `index.js`
- **Start**: `npm start` -> `node index.js`

### hello-world-csharp (CLI Tool)
- **Language**: C# 12 / .NET 8.0
- **Build**: `dotnet build`
- **Entry point**: `Program.cs` (top-level statements)
- **Output**: Console executable

## 3. Error Handling

- HTTP 400 for invalid parameters (bad name, unsupported locale)
- HTTP 404 for unknown routes
- HTTP 500 for unexpected server errors
- Errors return JSON: `{"error": "<message>"}`

## 4. Configuration

All services use environment variables for configuration:

| Variable | Default | Description |
|----------|---------|-------------|
| `PORT` | `8080` | HTTP listen port (Java, Node.js) |
| `GREETING_API_URL` | `http://localhost:8080` | API base URL (Node.js, C#) |
