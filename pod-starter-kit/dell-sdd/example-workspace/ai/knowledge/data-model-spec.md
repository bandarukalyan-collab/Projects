# Data Model Specification — Greeting Service

> Auto-generated by `/create-pod-knowledge`. Updated by `/update-knowledge`.

## 1. Overview

The Greeting Service currently uses no persistent storage — greetings are computed on the fly. This spec documents the planned data model for locale-based greeting templates.

## 2. Entities

### GreetingTemplate *(planned)*

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | String (UUID) | Yes | Unique identifier |
| `locale` | String | Yes | Language/region code (e.g., `en-US`) |
| `template` | String | Yes | Greeting format string (e.g., `"Hello, {name}!"`) |
| `createdAt` | DateTime | Yes | Creation timestamp |
| `updatedAt` | DateTime | Yes | Last modification timestamp |

### Relationships

```
GreetingTemplate (1 per locale)
    +-- locale (unique index)
```

## 3. Indexes

| Collection | Index | Type | Purpose |
|-----------|-------|------|---------|
| greeting_templates | `locale` | Unique | Fast lookup by locale |
