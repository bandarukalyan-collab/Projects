# Deployment Specification — Greeting Service

> Auto-generated by `/create-pod-knowledge`. Updated by `/update-knowledge`.

## 1. Overview

Each service is built and deployed independently. Currently all three are simple executables with no container orchestration.

## 2. Build Artifacts

| Service | Build Command | Artifact |
|---------|--------------|----------|
| hello-world-java | `mvn clean package` | `target/hello-world-java-1.0-SNAPSHOT.jar` |
| hello-world-nodejs | `npm install` | `node_modules/` + `index.js` |
| hello-world-csharp | `dotnet publish` | `bin/Release/net8.0/publish/` |

## 3. Environments

| Environment | Purpose | Notes |
|-------------|---------|-------|
| Local | Developer workstation | All services run on localhost |
| Dev | Integration testing | *(planned)* |
| QA | Pre-production validation | *(planned)* |
| Prod | Production | *(planned)* |

## 4. CI/CD

- **Platform**: GitLab CI
- **Pipeline**: *(to be configured per project)*
- **Branching**: Feature branches merge into `main` via MR
