# Security Specification — Greeting Service

> Auto-generated by `/create-pod-knowledge`. Updated by `/update-knowledge`.

## 1. Overview

The Greeting Service is a demo application with minimal security requirements. This spec documents the baseline and planned security controls.

## 2. Authentication

- **Current**: None (open API)
- **Planned**: OAuth2 / API key authentication for production deployment

## 3. Authorization

- **Current**: No authorization model — all endpoints are public
- **Planned**: Role-based access for administrative endpoints (template management)

## 4. Input Validation

| Parameter | Validation | Error |
|-----------|-----------|-------|
| `name` | 1-100 chars, no HTML/script tags | 400 Bad Request |
| `locale` | Must match supported locale list | 400 Bad Request |

## 5. Secrets Management

- No secrets in source code
- Service credentials stored in environment variables or `local.config`
- `local.config` is gitignored — never committed

## 6. OWASP Considerations

| Risk | Mitigation |
|------|-----------|
| Injection | Input validation on all parameters |
| XSS | JSON-only responses (no HTML rendering in API) |
| Sensitive Data Exposure | No PII stored or processed |
