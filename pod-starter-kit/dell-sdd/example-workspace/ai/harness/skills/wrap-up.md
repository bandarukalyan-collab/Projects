# wrap-up

> Clean up feature branches after all MRs are merged, publish comprehensive Confluence documentation, post a coding summary to JIRA, and transition the issue to Complete.

---

## Purpose

This workflow performs post-merge wrap-up for a JIRA feature issue. It verifies that all Merge Requests across impacted projects have been merged, deletes the corresponding feature branches (local and remote), **publishes comprehensive functional and technical documentation to Confluence** under a configurable parent page (mandatory — every feature must have a Confluence page), posts a comprehensive coding summary comment to the JIRA issue in wiki markup, and transitions the issue to "Complete" or "Done". Knowledge base integration (AGENTS.md, knowledge specs, LEARNINGS.md) is handled separately by the `update-knowledge` skill.

**Note:** The per-feature change summary (`ai/specs/{ISSUE_ID}/change-summary.md`) is generated by the `/execute` skill and includes the traceability matrix, task progress checklist, modules affected, files modified, and test results. The `wrap-up` skill references this artifact but does not regenerate it.

**This skill does NOT merge anything.** All MRs must already be in the `merged` state before running this workflow. If any MR is still open, the workflow stops immediately.

---

## Prerequisites

- `local.config` at workspace root contains `JIRA_PAT` and `GIT_PAT`:
  ```
  JIRA_PAT=<Base64-encoded JIRA personal access token>
  GIT_PAT=<GitLab personal access token (glpat-...)>
  ```
- `local.config` must also contain Confluence credentials for publishing documentation:
  ```
  CONFLUENCE_PAT=<Base64-encoded Confluence personal access token>
  CONFLUENCE_PARENT=<Full URL of the parent Confluence page>
  ```
  If either `CONFLUENCE_PAT` or `CONFLUENCE_PARENT` is missing or empty, the workflow will **warn the user and prompt for configuration** before proceeding — Confluence documentation is a mandatory deliverable.
- Each git project has an `origin` remote pointing to a GitLab instance (derived dynamically from the remote URL).
- Feature branches follow the naming convention `develop-PROJECT-XXXX` (where `PROJECT-XXXX` is the JIRA issue ID).
- All Merge Requests for the feature must already be merged into `develop`.
- The JIRA issue exists and is accessible with the configured `JIRA_PAT`.

---

## Workflow Steps

### Step 1 — Obtain JIRA Issue ID

Determine the JIRA issue ID (`PROJECT-XXXX`) for this feature. Sources (in priority order):

1. The user provides it directly (e.g., "wrap up PROJECT-1234").
2. Detect from the current branch name: `git branch --show-current` → `develop-PROJECT-1234` → extract `PROJECT-1234`.
3. Prompt the user if it cannot be determined.

Store as `ISSUE_ID` (e.g., `PROJECT-1234`). The feature branch name pattern is `develop-{ISSUE_ID}` (e.g., `develop-PROJECT-1234`).

---

### Step 2 — Load Authentication

Read credentials from the workspace-root `local.config` file:

```bash
# WORKSPACE_ROOT should already be set; if not, use the current working directory
WORKSPACE_ROOT="${WORKSPACE_ROOT:-$(pwd)}"
JIRA_PAT=$(grep JIRA_PAT "$WORKSPACE_ROOT/local.config" | cut -d= -f2)
GIT_PAT=$(grep GIT_PAT "$WORKSPACE_ROOT/local.config" | cut -d= -f2)
CONFLUENCE_PAT=$(grep CONFLUENCE_PAT "$WORKSPACE_ROOT/local.config" | cut -d= -f2)
CONFLUENCE_PARENT=$(grep CONFLUENCE_PARENT "$WORKSPACE_ROOT/local.config" | cut -d= -f2-)
JIRA_BASE_URL=$(grep JIRA_BASE_URL "$WORKSPACE_ROOT/local.config" | cut -d= -f2)
CONFLUENCE_BASE_URL=$(grep CONFLUENCE_BASE_URL "$WORKSPACE_ROOT/local.config" | cut -d= -f2-)
```

**JIRA API auth** uses the PAT as a Bearer token:
```
Authorization: Bearer $JIRA_PAT
```

**GitLab API auth** uses the PAT as a private token header:
```
PRIVATE-TOKEN: $GIT_PAT
```

**Confluence API auth** uses the PAT as a Bearer token:
```
Authorization: Bearer $CONFLUENCE_PAT
```

**Base URLs:**
- JIRA: Read `JIRA_BASE_URL` from `local.config`
- GitLab: Derive from the project's `origin` remote URL
- Confluence: Read `CONFLUENCE_BASE_URL` from `local.config` (optional)

---

### Step 3 — Verify All MRs Are Merged

Before deleting any branches, verify that every project's MR for this feature is safely merged. **Do NOT proceed if any MR is still open.**

**Git projects to check** — dynamically discover all Git repositories under the workspace root:

1. Iterate over every top-level subdirectory under the workspace root.
2. For each subdirectory, check if it contains a `.git` directory (i.e. is a Git repository).
3. For each Git repository, determine the GitLab project path by reading the `origin` remote URL:

```bash
REMOTE_URL=$(git remote get-url origin)
# Derive GitLab host and API base from the remote URL
GIT_HOST=$(echo "$REMOTE_URL" | sed 's|https://\([^/]*\)/.*|\1|')
GIT_API_BASE="https://${GIT_HOST}/api/v4"
# Extract path: remove https://<host>/ prefix and .git suffix
PROJECT_PATH=$(echo "$REMOTE_URL" | sed 's|https://[^/]*/||; s|\.git$||')
# URL-encode: replace / with %2F
ENCODED_PATH=$(echo "$PROJECT_PATH" | sed 's|/|%2F|g')
```

**For each project directory:**

#### 3.1 — Check if the remote feature branch exists

```bash
cd <project-dir>
git fetch origin
git branch -r --list "origin/develop-${ISSUE_ID}"
```

If the command returns output, the remote branch exists. If empty, the remote branch does not exist.

#### 3.2 — Check if a local feature branch exists

```bash
git branch --list "develop-${ISSUE_ID}"
```

#### 3.3 — Check MR status via GitLab API

Query for **merged** MRs:

```bash
curl -s --header "PRIVATE-TOKEN: $GIT_PAT" \
  "${GIT_API_BASE}/projects/<ENCODED_PATH>/merge_requests?source_branch=develop-${ISSUE_ID}&target_branch=develop&state=merged"
```

Query for **opened** MRs:

```bash
curl -s --header "PRIVATE-TOKEN: $GIT_PAT" \
  "${GIT_API_BASE}/projects/<ENCODED_PATH>/merge_requests?source_branch=develop-${ISSUE_ID}&target_branch=develop&state=opened"
```

#### 3.4 — Classify each project

|| Remote branch? | MR state | Local branch? | Classification | Action |
||---|---|---|---|---|
|| Yes/No | `merged` | Yes/No | **Safe** | Proceed with branch deletion |
|| Yes/No | `opened` | Yes/No | **STOP** | Halt entire workflow — MR must be merged first |
|| Yes | No MR found | Yes/No | **Warn** | Remote branch exists but no MR — ask user to confirm deletion |
|| No | No MR found | Yes | **Safe** | Only local branch remains — safe to delete |
|| No | No MR found | No | **Skip** | No involvement with this feature — nothing to do |

#### 3.5 — Present summary for confirmation

Display a table summarizing all projects:

```
=== MR Verification Summary for {ISSUE_ID} ===

|| Project              | Remote Branch | Local Branch | MR Status | Classification |
||----------------------|---------------|--------------|-----------|----------------|
|| project-alpha        | YES           | YES          | merged    | Safe           |
|| project-beta         | YES           | YES          | merged    | Safe           |
|| project-gamma        | NO            | NO           | —         | Skip           |
|| project-delta        | NO            | YES          | —         | Safe           |
|| project-epsilon      | NO            | NO           | —         | Skip           |
|| project-zeta         | NO            | NO           | —         | Skip           |
```

**If any project has classification = STOP**, halt the entire workflow and report which MRs are still open. Do NOT delete any branches.

**If any project has classification = Warn**, present the warning and ask the user to confirm before proceeding.

---

### Step 4 — Delete Feature Branches

For each project classified as **Safe** or **Warn (confirmed)**, perform branch cleanup:

#### 4.1 — Switch to develop

```bash
cd <project-dir>
git checkout develop && git pull origin develop
```

**Error handling:** If `git checkout develop` fails (e.g., uncommitted changes):
```bash
git fetch origin develop
git checkout -f develop
git reset --hard origin/develop
```

#### 4.2 — Delete local feature branch

```bash
git branch -D develop-${ISSUE_ID}
```

**Error handling:**
- If the branch does not exist locally, that's fine — skip.
- If `-D` fails because it's "not fully merged" — this should NOT happen since we verified the MR is merged. **STOP and report** this anomaly. Do not force-delete without investigation.

#### 4.3 — Delete remote feature branch (if it exists)

```bash
git push origin --delete develop-${ISSUE_ID}
```

**Error handling:**
- If the remote branch was already deleted (error: `remote ref does not exist`) — that's fine, continue.
- If there's a permission error (403) — report the failure but continue with other projects. The user may need to delete it manually via GitLab UI.

#### 4.4 — Prune stale remote-tracking references

```bash
git fetch --prune
```

This removes any remaining `origin/develop-{ISSUE_ID}` remote-tracking references.

---

### Step 5 — Gather Coding Summary

Collect information for the JIRA summary comment from multiple sources:

#### 5.1 — Feature specification

If a feature spec exists at `ai/specs/{ISSUE_ID}/specs.md`, read it **in full** to extract:
- **Overview** (§4.2) — business summary of what the feature does and why it matters
- **Background & Context** (§4.3) — business problem, JIRA description, POD Spec References
- **Scope** (§4.4) — in-scope deliverables and out-of-scope exclusions
- **Functional Requirements** (§4.5) — numbered requirements table (ID | Requirement | Source)
- **Acceptance Criteria** (§4.6) — Given/When/Then or checklist format
- **Non-Functional Requirements** (§4.7) — performance, security, scalability, compatibility constraints
- **Impacted Areas** (§4.8) — table of areas, files/classes/endpoints, nature of change
- **Open Questions & Assumptions** (§4.9) — resolved questions and standing assumptions

#### 5.2 — Implementation plan

If an implementation plan exists at `ai/specs/{ISSUE_ID}/plan.md`, read it **in full** to extract:
- **Context** (§5.2) — technical approach, API endpoints involved, key technical challenges
- **Example Request & Response** (§5.2.1) — sample API payloads if applicable
- **Feature-Specific Logic** (§5.2.2) — business rules, merge strategies, routing logic, event schemas
- **Files to Modify** (§5.2.4) — detailed file list with paths, methods, and change descriptions
- **Files NOT to Modify** (§5.2.5) — explicit exclusions with reasoning
- **Implementation Order** (§5.2.6) — TDD steps, test case names and descriptions
- **Critical Constraints** (§5.2.7) — backward compatibility, reactive, atomicity, signing requirements
- **Key Code References** (§5.4) — reference table of files, methods, and line numbers

#### 5.3 — Git log for commits

For each project that had a merged MR, collect commits referencing this issue:

```bash
cd <project-dir>
git fetch origin develop
git log origin/develop --oneline --grep="JIRA#${ISSUE_ID}"
```

Also try alternative patterns:
```bash
git log origin/develop --oneline --grep="${ISSUE_ID}"
```

Record: commit hash, one-line message, author, date.

#### 5.4 — GitLab MR details

For each merged MR, fetch full details:

```bash
curl -s --header "PRIVATE-TOKEN: $GIT_PAT" \
  "${GIT_API_BASE}/projects/<ENCODED_PATH>/merge_requests?source_branch=develop-${ISSUE_ID}&target_branch=develop&state=merged"
```

Extract per MR:
- MR IID and title
- MR web URL
- Author
- Merge date (`merged_at`)
- Number of changes (files modified)

Fetch MR changes to get the list of files modified:

```bash
curl -s --header "PRIVATE-TOKEN: $GIT_PAT" \
  "${GIT_API_BASE}/projects/<ENCODED_PATH>/merge_requests/<MR_IID>/changes"
```

Extract from `changes[]`:
- File paths (`new_path`)
- Change type (added, modified, deleted — infer from `new_file`, `deleted_file`, `renamed_file` flags)

#### 5.5 — Test information

Note any test classes added or modified from the MR changes list (files matching `*Test.java`, `*Tests.java`, `*Spec.java`, `*IT.java`).

---

### Step 6 — Post Summary Comment to JIRA

Compose a JIRA comment in **wiki markup** and post it to the issue.

#### 6.1 — Wiki markup template

```
{panel:title=Feature Complete — {ISSUE_ID}|borderStyle=solid|borderColor=#00875a|titleBGColor=#00875a|titleColor=#ffffff|bgColor=#f0fff0}

h3. Summary

{summary_text}

h3. Merge Requests

|||Project||MR||Title||Author||Merged||Files Changed||
||{project_1}|[!{mr_iid_1}|{mr_url_1}]|{mr_title_1}|{mr_author_1}|{merged_date_1}|{file_count_1}|
||{project_2}|[!{mr_iid_2}|{mr_url_2}]|{mr_title_2}|{mr_author_2}|{merged_date_2}|{file_count_2}|

h3. Files Modified

|||Project||File||Change Type||
||{project}|{{file_path_1}}|Modified|
||{project}|{{file_path_2}}|Added|
||{project}|{{file_path_3}}|Deleted|

h3. Tests

|||Project||Test Class||Type||
||{project}|{{TestClassName1}}|Unit|
||{project}|{{TestClassName2}}|Integration|

h3. Commits

|||Project||Hash||Message||
||{project_1}|{{abc1234}}|{commit_message_1}|
||{project_1}|{{def5678}}|{commit_message_2}|
||{project_2}|{{ghi9012}}|{commit_message_3}|

h3. Artifacts Retained

* Feature spec: {{ai/specs/{ISSUE_ID}/specs.md}}
* Implementation plan: {{ai/specs/{ISSUE_ID}/plan.md}}
* Session logs retained in {{.devin/}} workspace directory

{panel}
```

**Substitution rules:**
- `{summary_text}` — 2–4 sentence summary of what was implemented, derived from the feature spec and MR titles.
- `{mr_url_N}` — Full GitLab web URL for the MR (e.g., `https://{git-host}/{project-path}/-/merge_requests/{id}`).
- `{merged_date_N}` — Format as `YYYY-MM-DD`.
- `{file_count_N}` — Number of files in the MR's `changes[]` array.
- Test type: infer `Integration` if file name contains `IT`, otherwise `Unit`.

#### 6.2 — Post the comment

```bash
curl -s --request POST \
  --header "Authorization: Bearer $JIRA_PAT" \
  --header "Content-Type: application/json" \
  --data '{
    "body": "<wiki_markup_content_escaped_for_json>"
  }' \
  "${JIRA_BASE_URL}/rest/api/2/issue/${ISSUE_ID}/comment"
```

**Error handling:**
- `200`/`201` — Success.
- `401`/`403` — Authentication failure. Report but do NOT fail the workflow. Branch cleanup is already done.
- `404` — Issue not found. Report.
- Network error — Report but do NOT fail the workflow.

#### 6.3 — Add `AI-Delivered` label

Add a static label `AI-Delivered` to the JIRA issue so AI-delivered work can be tracked for reporting purposes.

```bash
curl -s --request PUT \
  --header "Authorization: Bearer $JIRA_PAT" \
  --header "Content-Type: application/json" \
  --data '{
    "update": {
      "labels": [{"add": "AI-Delivered"}]
    }
  }' \
  "${JIRA_BASE_URL}/rest/api/2/issue/${ISSUE_ID}"
```

This uses the JIRA `update` field operation to **add** the label without removing any existing labels on the issue.

**Error handling:**
- `204` — Success (no content).
- `200` — Success.
- `401`/`403` — Authentication failure. Report but do NOT fail the workflow.
- `404` — Issue not found. Report.
- Network error — Report but do NOT fail the workflow.

**JIRA failure is NOT a workflow failure.** The primary goal (branch cleanup) has already been achieved by this point.

---

### Step 7 — Transition JIRA to Complete

Move the JIRA issue to the "Complete" (or "Done") status.

#### 7.1 — Fetch current issue status

```bash
curl -s --header "Authorization: Bearer $JIRA_PAT" \
  "${JIRA_BASE_URL}/rest/api/2/issue/${ISSUE_ID}?fields=status"
```

Check `fields.status.name`. If already "Complete" or "Done", skip the transition.

#### 7.2 — Get available transitions

```bash
curl -s --header "Authorization: Bearer $JIRA_PAT" \
  "${JIRA_BASE_URL}/rest/api/2/issue/${ISSUE_ID}/transitions"
```

Parse the `transitions[]` array. Find a transition where `name` matches (case-insensitive):
- `"Complete"`
- `"Done"`
- `"Close"`
- `"Resolve"`

Record the `transition.id`.

#### 7.3 — Execute the transition

```bash
curl -s --request POST \
  --header "Authorization: Bearer $JIRA_PAT" \
  --header "Content-Type: application/json" \
  --data '{
    "transition": {
      "id": "<TRANSITION_ID>"
    }
  }' \
  "${JIRA_BASE_URL}/rest/api/2/issue/${ISSUE_ID}/transitions"
```

**Error handling:**
- `204` — Success (no content).
- `401`/`403` — Permission denied. Report: "Could not transition {ISSUE_ID} — insufficient permissions. Please transition manually."
- No matching transition found — Report: "No 'Complete' or 'Done' transition available from current status '{current_status}'. Please transition manually."
- Network error — Report but do NOT fail the workflow.

**JIRA failure is NOT a workflow failure.** Branch cleanup has already been completed successfully.

---

### Step 8 — Commit `ai/` Repo Changes

The `ai/` directory is a separate Git repository. Commit any changes made during this session (e.g. updated specs, change-summary).

**Stage and commit:**

```bash
cd {WORKSPACE_ROOT}/ai
git add -A
git status
```

If there are staged changes, commit them:

```bash
git commit -m "JIRA#${ISSUE_ID}; Post wrap-up sync"
```
(Commit message format defined in `AGENTS.md`)

If there are no changes to commit, skip silently. If the `ai` repo is on `develop` (not a feature branch), push the commit directly.

---

### Step 9 — Knowledge Integration (deferred to `update-knowledge`)

After a feature is merged, the workspace knowledge base (AGENTS.md, knowledge specs, LEARNINGS.md) should reflect any functional changes introduced. **This is handled by the separate `/update-knowledge` skill**, which can be run periodically to batch-integrate multiple delivered features.

The `wrap-up` skill does **not** perform knowledge integration inline — it focuses on branch cleanup, Confluence publishing, and JIRA closure. Run `update-knowledge` after wrap-up (or periodically) to keep the knowledge base current.

---

### Step 10 — Publish Confluence Documentation Page

Create a comprehensive functional and technical documentation page on Confluence under the configured parent page. This page serves as the **canonical reference** for the delivered change — covering business context, functional requirements, architecture, implementation details, testing, and traceability. **This step is mandatory** — every delivered feature must have a Confluence documentation page.

**If Confluence credentials are missing:**

If `CONFLUENCE_PAT` or `CONFLUENCE_PARENT` is missing or empty in `local.config`:

```
=== Confluence Configuration Required ===

Confluence documentation is a mandatory step in the wrap-up workflow.
The following configuration is missing from local.config:

  - CONFLUENCE_PAT:    {MISSING / OK}
  - CONFLUENCE_PARENT:  {MISSING / OK}

Please add the missing values to local.config and re-run /wrap-up,
or provide them now.
```

**WAIT for user response.** If the user provides the values, update `local.config` and proceed. If the user explicitly asks to skip Confluence (e.g., "skip confluence"), log a warning and continue — but note the skip in the final report.

**If the Confluence API is unreachable** after credentials are configured, retry once after 5 seconds. If still unreachable, log a warning and continue — the documentation can be published later by re-running `/wrap-up`.

#### 10.1 — Parse parent page URL

Extract the **space key** and **parent page ID** from the `CONFLUENCE_PARENT` URL.

The URL follows the pattern: `${CONFLUENCE_BASE_URL}/spaces/{SPACE_KEY}/pages/{PAGE_ID}/{Page+Title}`

```bash
# Example: ${CONFLUENCE_BASE_URL}/spaces/DF/pages/1286841426/FY27+-+DL+Features+Delivery
SPACE_KEY=$(echo "$CONFLUENCE_PARENT" | grep -oP '/spaces/\K[^/]+')
PARENT_PAGE_ID=$(echo "$CONFLUENCE_PARENT" | grep -oP '/pages/\K[0-9]+')
```

If either `SPACE_KEY` or `PARENT_PAGE_ID` is empty after parsing, log a warning and skip this step.

#### 10.2 — Verify parent page exists

```bash
curl -s --header "Authorization: Bearer $CONFLUENCE_PAT" \
  "${CONFLUENCE_BASE_URL}/rest/api/content/${PARENT_PAGE_ID}?expand=space"
```

If the response is not `200`, log a warning ("Confluence parent page not accessible — skipping Confluence publish") and skip this step. Do NOT fail the workflow.

#### 10.3 — Generate page title

Construct the page title in the format:

```
{ISSUE_ID} - {Short Heading}
```

Where `{Short Heading}` is a **maximum 5-word** summary derived from the JIRA issue summary. Condense the JIRA summary into at most 5 words that capture the essence of the change.

Examples:
- JIRA summary "Additional Info Only Update Registration" → `PROJECT-1234 - AdditionalInfo Only Update Registration`
- JIRA summary "Fix entitlement count mismatch when multiple pools exist for same model" → `PROJECT-2345 - Fix Entitlement Count Mismatch`
- JIRA summary "Add hardware serial validation to registration endpoint" → `PROJECT-3456 - HW Serial Registration Validation`

#### 10.4 — Build page content

Compose the page body in **Confluence Storage Format** (XHTML). The page **must always contain all 13 sections listed below**, in this exact order and with these exact headings. This ensures every feature documentation page follows a consistent, predictable structure. If a section does not apply to the current change, include the section heading with a brief explanation (e.g., "No API changes were made as part of this feature.").

**Data sources** — all content must be derived from actual data gathered in Steps 5–8:
- Feature spec: `ai/specs/{ISSUE_ID}/specs.md` (§4.2–§4.9)
- Implementation plan: `ai/specs/{ISSUE_ID}/plan.md` (§5.2–§5.4)
- Git log, GitLab MR details, and MR file changes (Step 5.3–5.5)
- JIRA issue metadata (Step 8.2)

```html
<ac:structured-macro ac:name="info">
  <ac:rich-text-body>
    <p>This page is the canonical functional and technical reference for
    <a href="${JIRA_BASE_URL}/browse/{ISSUE_ID}"><strong>{ISSUE_ID}</strong></a>.
    Auto-generated by the SDLC wrap-up workflow after all merge requests were merged.</p>
  </ac:rich-text-body>
</ac:structured-macro>

<!-- ==================== SECTION 1: OVERVIEW ==================== -->
<h2>1. Overview</h2>
<table>
  <tbody>
    <tr><th>JIRA Issue</th><td><a href="${JIRA_BASE_URL}/browse/{ISSUE_ID}">{ISSUE_ID}</a></td></tr>
    <tr><th>Title</th><td>{Full JIRA issue summary}</td></tr>
    <tr><th>Type</th><td>{Feature / Defect / Task}</td></tr>
    <tr><th>Priority</th><td>{Priority from JIRA}</td></tr>
    <tr><th>Status</th><td>Complete</td></tr>
    <tr><th>Completion Date</th><td>{YYYY-MM-DD — date of the last MR merge}</td></tr>
    <tr><th>Projects Impacted</th><td>{comma-separated list of projects that had MRs}</td></tr>
    <tr><th>Modules Impacted</th><td>{comma-separated list of all Maven modules touched across all projects}</td></tr>
    <tr><th>Delivered By</th><td>AI-Assisted (Devin/Windsurf)</td></tr>
  </tbody>
</table>

<!-- ==================== SECTION 2: BACKGROUND ==================== -->
<h2>2. Background &amp; Business Context</h2>
<p>{Comprehensive description of the business problem this change addresses.
Why was this change needed? What user pain point, business requirement, or
system limitation does it solve? Who are the impacted stakeholders, systems,
or personas? Write 4-8 sentences aimed at a PM or stakeholder audience.
Derive from specs.md §4.2 (Overview) and §4.3 (Background &amp; Context).}</p>

<!-- ==================== SECTION 3: FUNCTIONAL REQUIREMENTS ==================== -->
<h2>3. Functional Requirements</h2>
<p>The following functional requirements were implemented:</p>
<table>
  <thead><tr><th>ID</th><th>Requirement</th><th>Source</th></tr></thead>
  <tbody>
    <tr><td>FR-01</td><td>{Requirement text — "The system SHALL ..."}</td><td>{Source — AC, JIRA description, stakeholder}</td></tr>
    {... one row per FR from specs.md §4.5 ...}
  </tbody>
</table>
<p><em>If no formal FRs exist in the feature spec, summarise the key behavioural
changes as numbered items in this table.</em></p>

<!-- ==================== SECTION 4: ACCEPTANCE CRITERIA ==================== -->
<h2>4. Acceptance Criteria</h2>
{If Given/When/Then format exists in specs.md §4.6:}
<ac:structured-macro ac:name="code">
  <ac:parameter ac:name="language">text</ac:parameter>
  <ac:plain-text-body><![CDATA[
GIVEN {precondition}
WHEN  {action}
THEN  {expected result}
  ]]></ac:plain-text-body>
</ac:structured-macro>
{If checklist format, use a numbered list. If no ACs were defined:}
<p><em>No formal acceptance criteria were defined on the JIRA issue.
The functional requirements above serve as the acceptance baseline.</em></p>

<!-- ==================== SECTION 5: SCOPE ==================== -->
<h2>5. Scope</h2>
<h3>In Scope</h3>
<ul>
  <li>{Deliverable 1 — from specs.md §4.4}</li>
  <li>{Deliverable 2}</li>
</ul>
<h3>Out of Scope</h3>
<ul>
  <li>{Excluded item 1 — from specs.md §4.4}</li>
  <li>{Excluded item 2}</li>
</ul>

<!-- ==================== SECTION 6: TECHNICAL ARCHITECTURE ==================== -->
<h2>6. Technical Architecture</h2>

<h3>6.1 Technical Approach</h3>
<p>{Detailed description of the technical approach. Which architectural
patterns were leveraged (handler → service → repository, async/reactive
chains, event outbox, correlation ID propagation, etc.)?
What was the key technical challenge and how was it solved? Which layers
of the stack were modified? Derive from plan.md §5.2
(Context) and §5.2.2 (feature-specific logic). Write 4-8 sentences
aimed at peer engineers.}</p>

<h3>6.2 Processing Flow</h3>
<p>End-to-end processing flow for this change:</p>
<ol>
  <li><strong>{Step label}</strong> — {Description of what happens, including the
  class/method responsible. e.g., "Request received —
  RequestHandler.handleUpdate() receives the request
  and delegates to the service layer."}</li>
  <li><strong>{Step label}</strong> — {e.g., "Validation —
  RequestValidator.validateUpdate() checks request
  validity; new branch bypasses certain
  validation."}</li>
  <li><strong>{Step label}</strong> — {e.g., "Service processing —
  DomainService.processUpdate() detects the
  update path and performs merge logic."}</li>
  <li><strong>{Step label}</strong> — {e.g., "Persistence — Merged entity
  saved to the database via DomainRepository."}</li>
  <li><strong>{Step label}</strong> — {e.g., "Response — Response DTO built,
  computed fields injected, response signed,
  transaction event published to the event outbox."}</li>
  {... as many steps as needed to describe the full request/event flow ...}
</ol>
<ac:structured-macro ac:name="note">
  <ac:rich-text-body>
    <p><strong>Entry point:</strong> <code>{HTTP method} {endpoint path}</code>
    &#x2192; <code>{HandlerClass}</code>
    &#x2192; <code>{ServiceClass}</code>
    &#x2192; <code>{Repository / DB}</code></p>
  </ac:rich-text-body>
</ac:structured-macro>

<!-- ==================== SECTION 6.2a: ARCHITECTURE SEQUENCE DIAGRAM ==================== -->
<h3>6.2a Architecture Sequence Diagram</h3>
<p>The following sequence diagram illustrates the end-to-end architecture and data flow for this feature:</p>
<ac:structured-macro ac:name="plantuml">
  <ac:plain-text-body><![CDATA[@startuml
{PlantUML sequence diagram — see §10.4a generation rules below}
@enduml]]></ac:plain-text-body>
</ac:structured-macro>

<h3>6.3 Impacted Components</h3>
<table>
  <thead><tr><th>Layer</th><th>Component</th><th>Project</th><th>Nature of Change</th></tr></thead>
  <tbody>
    <tr>
      <td>{API / Handler / Service / Repository / Model / Config / Test}</td>
      <td><code>{ClassName}</code></td>
      <td>{project-name}</td>
      <td>{Brief description of change}</td>
    </tr>
    {... one row per impacted component, from specs.md §4.8
    cross-referenced with plan.md §5.2.4 ...}
  </tbody>
</table>

<h3>6.4 Key Design Decisions</h3>
<table>
  <thead><tr><th>#</th><th>Decision</th><th>Rationale</th><th>Alternatives Considered</th></tr></thead>
  <tbody>
    <tr>
      <td>1</td>
      <td>{Decision statement}</td>
      <td>{Why this approach was chosen}</td>
      <td>{What alternatives were considered, or "None"}</td>
    </tr>
    {... 3-6 rows. Derive from plan.md feature-specific logic,
    critical constraints, and any "Files NOT to Modify" reasoning.
    Focus on non-obvious choices a future developer would want to understand. ...}
  </tbody>
</table>

<h3>6.5 Assumptions &amp; Resolved Questions</h3>
<table>
  <thead><tr><th>#</th><th>Question / Assumption</th><th>Resolution</th></tr></thead>
  <tbody>
    <tr>
      <td>1</td>
      <td>{Question or assumption from specs.md §4.9}</td>
      <td>{How it was resolved}</td>
    </tr>
    {... one row per resolved question and standing assumption ...}
  </tbody>
</table>
<p><em>If none: "No open questions or assumptions were identified during planning."</em></p>

<h3>6.6 Critical Constraints</h3>
<ul>
  <li><strong>{Constraint name}</strong> — {Description. e.g.,
  "BACKWARD COMPATIBILITY — Existing update registration requests
  without additionalInfo must continue to work identically."}</li>
  {... from plan.md §5.2.7 ...}
</ul>

<!-- ==================== SECTION 7: API CHANGES ==================== -->
<h2>7. API Changes</h2>
{If the feature involves API endpoint changes:}
<table>
  <tbody>
    <tr><th>Method</th><td><code>{HTTP method}</code></td></tr>
    <tr><th>Path</th><td><code>{endpoint path}</code></td></tr>
    <tr><th>Change Type</th><td>{New endpoint / Modified behaviour / New query parameter / New request field / etc.}</td></tr>
    <tr><th>Description</th><td>{What changed in the endpoint behaviour}</td></tr>
  </tbody>
</table>

{If example request/response available from plan.md §5.2.1:}
<h3>Example Request</h3>
<ac:structured-macro ac:name="code">
  <ac:parameter ac:name="language">json</ac:parameter>
  <ac:plain-text-body><![CDATA[
{JSON request — abbreviated, with comments for notable fields}
  ]]></ac:plain-text-body>
</ac:structured-macro>

<h3>Example Response</h3>
<ac:structured-macro ac:name="code">
  <ac:parameter ac:name="language">json</ac:parameter>
  <ac:plain-text-body><![CDATA[
{JSON response — abbreviated, with comments for notable fields}
  ]]></ac:plain-text-body>
</ac:structured-macro>

{If no API changes:}
<p>No API changes were made as part of this feature.</p>

<!-- ==================== SECTION 8: DATA MODEL CHANGES ==================== -->
<h2>8. Data Model Changes</h2>
{If database collection/table, field/column, or index changes were made:}
<table>
  <thead><tr><th>Collection / Table</th><th>Field / Index</th><th>Change</th><th>Description</th></tr></thead>
  <tbody>
    <tr>
      <td><code>{collection_name}</code></td>
      <td><code>{field_name or index_name}</code></td>
      <td>{Added / Modified / Removed / New Index}</td>
      <td>{Description of the change}</td>
    </tr>
  </tbody>
</table>
{If no data model changes:}
<p>No data model changes were made as part of this feature.
Existing database collections/tables and indexes are used as-is.</p>

<!-- ==================== SECTION 9: IMPLEMENTATION DETAILS ==================== -->
<h2>9. Implementation Details</h2>
{For each project that had a merged MR, include a numbered sub-section:}

<h3>9.{N} {project-name}</h3>
<p>
  <strong>Merge Request:</strong> <a href="{mr_web_url}">!{iid} — {mr_title}</a><br/>
  <strong>Merged:</strong> {YYYY-MM-DD} &nbsp;|&nbsp; <strong>Author:</strong> {mr_author}<br/>
  <strong>Modules:</strong> {comma-separated list of Maven modules touched}
</p>
<table>
  <thead><tr><th>File</th><th>Module</th><th>Change</th><th>Description</th></tr></thead>
  <tbody>
    <tr>
      <td><code>{file_name}</code></td>
      <td>{module-name}</td>
      <td>{Added | Modified | Deleted}</td>
      <td>{One-line functional description of what changed in this file}</td>
    </tr>
    {... repeat for each file. Limit to 25 most significant files per project. ...}
  </tbody>
</table>
{If more than 25 files: "<p><em>... and {N} more files</em></p>"}

<!-- ==================== SECTION 10: TESTING ==================== -->
<h2>10. Testing</h2>

<h3>Test Strategy</h3>
<p>{Describe the testing approach: TDD (tests written first?), what kinds
of tests were added (unit, integration, E2E), which scenarios are covered,
how edge cases were validated. Derive from plan.md §5.2.6.
Write 2-4 sentences.}</p>

<h3>Test Cases</h3>
<table>
  <thead><tr><th>Type</th><th>Test Class</th><th>Test Method / Scenario</th><th>Project</th><th>Description</th></tr></thead>
  <tbody>
    <tr>
      <td>{Unit / Integration / E2E}</td>
      <td><code>{TestClassName}</code></td>
      <td><code>{testMethodName}</code></td>
      <td>{project}</td>
      <td>{What the test verifies}</td>
    </tr>
    {... one row per test method or scenario. Include all tests from
    plan.md §5.2.6 and any additional test files from MR changes. ...}
  </tbody>
</table>

<!-- ==================== SECTION 11: NON-FUNCTIONAL REQUIREMENTS ==================== -->
<h2>11. Non-Functional Requirements</h2>
{If NFRs were defined in specs.md §4.7:}
<table>
  <thead><tr><th>Category</th><th>Requirement</th><th>How Addressed</th></tr></thead>
  <tbody>
    <tr>
      <td>{Performance / Security / Scalability / Compatibility / Observability}</td>
      <td>{Requirement text}</td>
      <td>{How this was addressed in the implementation}</td>
    </tr>
  </tbody>
</table>
{If no NFRs defined:}
<p>No specific non-functional requirements were defined for this change.
Standard platform NFRs apply (as documented in the project's technical spec).</p>

<!-- ==================== SECTION 12: MRs & COMMITS ==================== -->
<h2>12. Merge Requests &amp; Commits</h2>

<h3>Merge Requests</h3>
<table>
  <thead><tr><th>Project</th><th>MR</th><th>Title</th><th>Author</th><th>Merged</th><th>Files Changed</th></tr></thead>
  <tbody>
    <tr>
      <td>{project}</td>
      <td><a href="{mr_url}">!{iid}</a></td>
      <td>{title}</td>
      <td>{author}</td>
      <td>{YYYY-MM-DD}</td>
      <td>{file_count}</td>
    </tr>
  </tbody>
</table>

<h3>Commits</h3>
<table>
  <thead><tr><th>Project</th><th>Hash</th><th>Message</th><th>Date</th></tr></thead>
  <tbody>
    <tr>
      <td>{project}</td>
      <td><code>{short_hash}</code></td>
      <td>{commit_message}</td>
      <td>{YYYY-MM-DD}</td>
    </tr>
    {... all commits referencing this ISSUE_ID ...}
  </tbody>
</table>

<!-- ==================== SECTION 13: REFERENCES ==================== -->
<h2>13. References &amp; Artifacts</h2>
<table>
  <thead><tr><th>Artifact</th><th>Location</th><th>Description</th></tr></thead>
  <tbody>
    <tr><td>JIRA Issue</td><td><a href="${JIRA_BASE_URL}/browse/{ISSUE_ID}">{ISSUE_ID}</a></td><td>Original issue with description, acceptance criteria, and comments</td></tr>
    <tr><td>Feature Spec</td><td><code>ai/specs/{ISSUE_ID}/specs.md</code></td><td>Detailed functional requirements specification</td></tr>
    <tr><td>Implementation Plan</td><td><code>ai/specs/{ISSUE_ID}/plan.md</code></td><td>Technical implementation plan with file references and test strategy</td></tr>
    <tr><td>Change Summary</td><td><code>ai/specs/{ISSUE_ID}/change-summary.md</code></td><td>Post-implementation summary of all changes made</td></tr>
    <tr><td>Functional Spec</td><td><code>ai/knowledge/functional-spec.md</code></td><td>Platform functional specification</td></tr>
    <tr><td>API Spec</td><td><code>ai/knowledge/api-spec.md</code></td><td>REST API endpoint reference</td></tr>
    <tr><td>Data Model Spec</td><td><code>ai/knowledge/data-model-spec.md</code></td><td>Data model and collection reference</td></tr>
    <tr><td>Technical Spec</td><td><code>ai/knowledge/technical-spec.md</code></td><td>Platform architecture and infrastructure reference</td></tr>
  </tbody>
</table>
```

**Content rules:**
- **All 13 sections are mandatory.** If a section does not apply (e.g., no API changes, no data model changes), keep the heading and add a one-line explanation (e.g., "No API changes were made as part of this feature."). Never omit a section.
- All text must be derived from actual data gathered in Steps 5–8. Do not invent or hallucinate details.
- **Section 2 (Background)** should be business-oriented and non-technical — suitable for a PM or stakeholder audience.
- **Section 6 (Technical Architecture)** should be developer-oriented — suitable for peer engineers reviewing the architecture.
- **Section 6.2 (Processing Flow)** must describe the end-to-end request/event flow as a numbered sequence, naming the specific classes and methods at each step. This acts as a textual architecture flow diagram.
- **Section 6.4 (Key Design Decisions)** should focus on non-obvious choices — things a future developer would want to know. Include rationale and alternatives considered.
- **Section 6.5 (Assumptions & Resolved Questions)** should include all resolved questions from the feature spec to preserve the decision-making context.
- File descriptions in the "Implementation Details" tables should be one-line functional summaries (e.g., "Added null-removal logic for additionalInfo merge"), not raw diffs.
- Limit file tables to 25 rows per project. If more files were changed, add a final row: `<tr><td colspan="4"><em>... and {N} more files</em></td></tr>`.
- **Section 7 (API Changes)** should include example request/response JSON when available from the implementation plan.
- Test case descriptions should explain what each test verifies, not just repeat the method name.
- **Section 6.2a (Architecture Sequence Diagram)** must contain a PlantUML sequence diagram generated according to the rules in §10.4a below.

#### 10.4a — Generate the PlantUML sequence diagram for Section 6.2a

The Confluence `plantuml` macro renders PlantUML source into a diagram inline. You **must** generate a meaningful sequence diagram for every feature — it is not optional.

**Data sources** (in priority order):
1. `ai/specs/{ISSUE_ID}/plan.md` — §5.2.3 (processing flow), §5.2.1 (architecture overview)
2. `ai/specs/{ISSUE_ID}/specs.md` — §4.3 (functional flow), §4.4 (business rules)
3. `ai/specs/{ISSUE_ID}/change-summary.md` — actual classes/methods implemented
4. MR diffs gathered in Step 5 — real class names and call chains

**Diagram construction rules:**
1. Use `@startuml` / `@enduml` delimiters.
2. Diagram type must be a **sequence diagram** (`participant`, `->`, `activate`/`deactivate`).
3. Define participants for each distinct layer or component involved: HTTP client, handler/router, service classes, repositories, external systems (databases, message brokers, caches, external APIs).
4. Use real class names from the implementation (e.g., `RegistrationService`, `EntitlementRepository`), not generic placeholders.
5. Model the **primary happy-path flow** end-to-end. Include key branching only if the feature has distinct conditional paths (e.g., "new vs existing registration").
6. Use `activate`/`deactivate` to show processing scope per participant.
7. Use `note over` or `note right` for important business rules or transformations at key steps.
8. Group related steps with `group` or `alt`/`else` blocks where appropriate.
9. Keep the diagram readable — aim for 20–60 lines of PlantUML source. For complex features with multiple distinct flows, use multiple `group` blocks within a single diagram rather than multiple diagrams.
10. Use the `skinparam` block for consistent styling:
```
skinparam sequenceArrowThickness 2
skinparam maxMessageSize 200
skinparam sequenceParticipant underline
```

**Example** (abbreviated):
```
@startuml
skinparam sequenceArrowThickness 2
skinparam maxMessageSize 200

participant "Client" as C
participant "RequestHandler" as IH
participant "DomainService" as RS
participant "DependencyService" as ES
participant "Database" as DB

C -> IH : {method} {endpoint}
activate IH
IH -> RS : processRequest(request)
activate RS
RS -> ES : findRelatedEntities(criteria)
activate ES
ES -> DB : query {collection}
DB --> ES : results
deactivate ES
RS -> DB : save {collection}
DB --> RS : saved
RS --> IH : Response
deactivate RS
IH --> C : 200 OK
deactivate IH
@enduml
```

Replace the `{PlantUML sequence diagram — see §10.4a generation rules below}` placeholder in the Section 6.2a template with the actual generated PlantUML source (without the outer ` ``` ` fences — the `<![CDATA[...]]>` block in the macro handles escaping).

#### 10.5 — Create or update the Confluence page

**First, check if a page with this title already exists** under the parent:

```bash
curl -s --header "Authorization: Bearer $CONFLUENCE_PAT" \
  "${CONFLUENCE_BASE_URL}/rest/api/content?title=$(python3 -c 'import urllib.parse; print(urllib.parse.quote("'"$PAGE_TITLE"'"))')&spaceKey=${SPACE_KEY}&type=page"
```

Parse the response: if `results[]` is non-empty, an existing page was found. Extract its `id` and `version.number`.

**If the page already exists — update it:**

```bash
EXISTING_PAGE_ID=<extracted id>
CURRENT_VERSION=<extracted version.number>
NEW_VERSION=$((CURRENT_VERSION + 1))

curl -s --request PUT \
  --header "Authorization: Bearer $CONFLUENCE_PAT" \
  --header "Content-Type: application/json" \
  --data '{
    "type": "page",
    "title": "<PAGE_TITLE>",
    "version": {"number": <NEW_VERSION>},
    "body": {
      "storage": {
        "value": "<ESCAPED_HTML_CONTENT>",
        "representation": "storage"
      }
    }
  }' \
  "${CONFLUENCE_BASE_URL}/rest/api/content/${EXISTING_PAGE_ID}"
```

**If the page does not exist — create it:**

```bash
curl -s --request POST \
  --header "Authorization: Bearer $CONFLUENCE_PAT" \
  --header "Content-Type: application/json" \
  --data '{
    "type": "page",
    "title": "<PAGE_TITLE>",
    "ancestors": [{"id": <PARENT_PAGE_ID>}],
    "space": {"key": "<SPACE_KEY>"},
    "body": {
      "storage": {
        "value": "<ESCAPED_HTML_CONTENT>",
        "representation": "storage"
      }
    }
  }' \
  "${CONFLUENCE_BASE_URL}/rest/api/content"
```

**Important:** The HTML content in `body.storage.value` must be properly JSON-escaped (escape double quotes, backslashes, newlines).

#### 10.6 — Error handling

| Response | Action |
|---|---|
| `200` | Success (create or update). Record the page URL from `response._links.webui` for the final report. |
| `401`/`403` | Auth failure. Log warning: "Confluence auth failed — skipping page publish." Continue workflow. |
| `404` | Parent page not found. Log warning. Continue workflow. |
| Network error | Log warning. Continue workflow. |

**Confluence API failure is non-blocking** — if the API is unreachable after retries, log the error and continue. The user can re-run `/wrap-up` to retry Confluence publishing. However, missing Confluence credentials should be resolved before proceeding (see the prompt above).

---

### Step 11 — Report Results

Produce a final summary covering all actions taken.

```
=== wrap-up Results for {ISSUE_ID} ===

--- Branch Cleanup ---

|| Project              | Local Deleted | Remote Deleted | Status  |
||----------------------|---------------|----------------|---------|
|| project-alpha        | YES           | YES            | Clean   |
|| project-beta         | YES           | YES            | Clean   |
|| project-gamma        | —             | —              | Skipped |
|| project-delta        | YES           | —              | Clean   |
|| project-epsilon      | —             | —              | Skipped |
|| project-zeta         | —             | —              | Skipped |

--- JIRA Update ---

  Issue:       {ISSUE_ID}
  Comment:     Posted (or: FAILED — <reason>)
  Label:       AI-Delivered added (or: FAILED — <reason>)
  Transition:  Complete (or: FAILED — <reason> / Already in status "Done")

--- Confluence Documentation ---

  Page:   {PAGE_TITLE}
  URL:    {confluence_page_url}
  Action: Created (or: Updated existing page)
  (or: Skipped — CONFLUENCE_PARENT not configured)
  (or: FAILED — <reason>)

--- Knowledge Integration ---

  Deferred to `update-knowledge` skill. Run it periodically to batch-integrate delivered features.

--- Summary ---

  Feature branches cleaned across N projects.
  M commits across N merge requests.
  JIRA issue updated and transitioned to Complete.
  Confluence documentation published (or: skipped).
  Knowledge integration: deferred to update-knowledge.

--- Artifacts Retained ---

  - ai/specs/{ISSUE_ID}/specs.md
  - ai/specs/{ISSUE_ID}/plan.md
  - ai/specs/{ISSUE_ID}/change-summary.md
  - .devin/ session artifacts
  - Git history preserved on develop branch
```

---

## Important Rules

1. **Never merge anything.** This skill only runs AFTER all MRs are already merged.
2. **Always verify MR status before deleting branches.** If any MR is still open, halt completely.
3. **Use `git branch -D`** (force delete) for local branches since the MR is confirmed merged.
4. **If `-D` fails with "not fully merged"**, STOP — this indicates an anomaly that needs investigation.
5. **JIRA failures do not block the workflow.** Branch cleanup is the primary objective. JIRA updates are best-effort.
6. **Present the verification summary** (Step 3.5) and get confirmation before deleting any branches.
7. **Do not delete the `develop` branch** — only delete `develop-{ISSUE_ID}` feature branches.
8. **Preserve artifacts.** Do not delete feature specs, implementation plans, or any files in the working tree. Only git branches are cleaned up.
9. **Never force-push.** Only delete branches and prune.
10. **`ai/` repo commit failure is non-blocking.** If committing to the `ai/` repo fails, report and continue. The primary objectives (branch cleanup, JIRA update) take priority.
11. **Confluence documentation is mandatory.** If Confluence credentials are missing, prompt the user to provide them. If the user explicitly requests to skip, allow it but log a warning. If the Confluence API is unreachable after retries, log the error and continue — the user can re-run `/wrap-up` to publish later. If a page already exists, update it in place.
12. **Knowledge integration is deferred.** This skill does not update AGENTS.md, knowledge specs, or LEARNINGS.md. Use the `update-knowledge` skill for that.

---

## API Reference

### GitLab

|| Action | Method | Endpoint |
||---|---|---|
|| List merged MRs | `GET` | `/projects/:id/merge_requests?source_branch=:branch&target_branch=develop&state=merged` |
|| List open MRs | `GET` | `/projects/:id/merge_requests?source_branch=:branch&target_branch=develop&state=opened` |
|| Get MR details | `GET` | `/projects/:id/merge_requests/:iid` |
|| Get MR changes | `GET` | `/projects/:id/merge_requests/:iid/changes` |

**Base URL**: Derived from the project's `origin` remote URL (see Step 3): `${GIT_API_BASE}`
**Auth header**: `PRIVATE-TOKEN: <GIT_PAT from local.config>`
**Project ID**: Use URL-encoded project path (see table in Step 3).

### JIRA

|| Action | Method | Endpoint |
||---|---|---|
|| Get issue | `GET` | `/rest/api/2/issue/:issueKey` |
|| Get issue status | `GET` | `/rest/api/2/issue/:issueKey?fields=status` |
|| Add comment | `POST` | `/rest/api/2/issue/:issueKey/comment` |
|| Add label | `PUT` | `/rest/api/2/issue/:issueKey` (body: `{"update":{"labels":[{"add":"AI-Delivered"}]}}`) |
|| Get transitions | `GET` | `/rest/api/2/issue/:issueKey/transitions` |
|| Execute transition | `POST` | `/rest/api/2/issue/:issueKey/transitions` |

**Base URL**: Read from `local.config` as `JIRA_BASE_URL`
**Auth header**: `Authorization: Bearer <JIRA_PAT from local.config>`

### Confluence

|| Action | Method | Endpoint |
||---|---|---|
|| Get page by ID | `GET` | `/rest/api/content/:pageId?expand=space` |
|| Search pages by title | `GET` | `/rest/api/content?title=:title&spaceKey=:key&type=page` |
|| Create page | `POST` | `/rest/api/content` |
|| Update page | `PUT` | `/rest/api/content/:pageId` |

**Base URL**: Read from `local.config` as `CONFLUENCE_BASE_URL`
**Auth header**: `Authorization: Bearer <CONFLUENCE_PAT from local.config>`
**Parent page**: Extracted from `CONFLUENCE_PARENT` URL in `local.config`.
