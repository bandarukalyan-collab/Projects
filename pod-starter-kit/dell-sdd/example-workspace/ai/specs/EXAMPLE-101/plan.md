# Implementation Plan — EXAMPLE-101: Add Personalized Greeting

> Generated by `/create-plan`. This is a **template example** showing the expected format.

## 1. Design Decisions

| # | Decision | Rationale |
|---|----------|-----------|
| D-1 | Parse `name` from query string in `App.java` | Single entry point, minimal change |
| D-2 | Validate name length (1-100 chars) | Prevent abuse and buffer issues |
| D-3 | Return 400 JSON error for invalid name | Consistent with planned error handling |

## 2. Files to Modify

| # | File | Change | FR |
|---|------|--------|-----|
| 1 | `hello-world-java/src/main/java/com/example/App.java` | Add name parameter parsing and greeting logic | FR-1, FR-2, FR-3, FR-4 |

## 3. Files NOT to Modify

- `hello-world-nodejs/` — No changes needed (frontend update is a separate story)
- `hello-world-csharp/` — No changes needed (CLI update is a separate story)

## 4. Implementation Order

### Step 1: Write unit tests (TDD)
- Test: default greeting returns `"Hello, World!"`
- Test: `name=Alice` returns `"Hello, Alice!"`
- Test: empty name returns 400

### Step 2: Implement greeting logic
- Parse `name` query parameter from request
- If present and valid: return `"Hello, {name}!"`
- If absent: return `"Hello, World!"`
- If invalid: return 400 error

### Step 3: Run tests and verify

## 5. Traceability Matrix

| FR | AC | File | Test |
|----|-----|------|------|
| FR-1 | AC-1 | App.java | testPersonalizedGreeting |
| FR-2 | AC-1 | App.java | testPersonalizedGreeting |
| FR-3 | AC-2 | App.java | testDefaultGreeting |
| FR-4 | AC-3 | App.java | testEmptyNameReturns400 |
